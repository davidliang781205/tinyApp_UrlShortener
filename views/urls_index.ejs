<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/_header %>
</head>
<body>
    <!-- Add your EJS code here -->
    <div class="container">
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
            <form method="POST" action="/urls/9sm5xK/delete">
                <table class="table">
                    <thead>
                    <tr>
                        <th>Short Key</th>
                        <th>Long URL</th>
                        <th>Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    <tr>
                    <% for (url in urls) { %>
                    <td><%= url %></td><td> <%= urls[url] %></td><td><button type="button" class="btn btn-warning">Delete</button></td></tr><tr>
                    <% } %>
                    </tr>
                    </tbody>
                </table>
            </form>
            </div>
            <div class="col-md-3"></div>
        </div>
    <div>
</body>
<footer>
        <% include partials/_footer %>
</footer>
</html>